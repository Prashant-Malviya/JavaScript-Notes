let firstPerson = {
    name : 'Mohan',
    age : 23
}

let secondPerson = firstPerson;
secondPerson.name = 'krishna'
console.log(firstPerson);
console.log(secondPerson);

// we have following reference Data types <----------

// Objects
// Arrays
// function

// in reference data types values stored in heap memory and their pointers stores in  stack memory that's by they are mutable <------------